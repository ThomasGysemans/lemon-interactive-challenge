<input
    class="main-input"
    type="{{ type }}"
    placeholder="{{ placeholder }}"
    maxlength="{{ maxLength }}"
    minlength="{{ minLength }}"
    spellcheck="{{ spellcheck ? 'true' : 'false' }}"
    autocomplete="{{ autocomplete }}"
    value="{{ value }}"
    name="{{ name }}"
    required
/>